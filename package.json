{
  "name": "ytboost",
  "version": "1.0.0",
  "description": "Full stack app",
  "engines": {
    "node": ">=18.0.0"
  },
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"python3 -m uvicorn backend.server:app --host 0.0.0.0 --port 8080\" \"cd frontend && npm start\"",
    "frontend": "cd frontend && npm start",
    "backend": "python3 -m uvicorn backend.server:app --host 0.0.0.0 --port 8080"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
